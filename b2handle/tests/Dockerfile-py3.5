# vim:set ft=dockerfile:
FROM       eudat-b2handle:py3.5

RUN        pip3 install \
           mock \
           coverage \
           nose

VOLUME     /opt/B2HANDLE/b2handle/tests

WORKDIR    /opt/B2HANDLE/b2handle/tests

COPY       docker-entrypoint.sh ./

ENTRYPOINT ["./docker-entrypoint.sh"]

CMD        ["coverage"]
